version: '3.1'
services:
    php-apache-environment:
        container_name: php-apache
        build:
            context: ./php
            dockerfile: Dockerfile
        depends_on:
            - db
        volumes:
            - ./php/src:/var/www/html/
        ports:
            - 8000:80
    db:
        container_name: db
        image: mysql
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: akEWDT_vGC-36BJjc6AX
            MYSQL_DATABASE: LaGazetteTulliste
            MYSQL_USER: elliot
            MYSQL_PASSWORD: U6vFJGsA2nydLPA9.d.L
            ports:
                - 9906:3306
    phpmyadmin:
        image: phpmyadmin
        restart: always
        ports:
            - 8687:80
        environment:
            - PMA_ARBITRARY=1

